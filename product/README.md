# 产品

> 由 `product` agent 独占写。其余角色只读。

这个目录目前是空的——产品线尚未启动，本轮的启动任务是 T-004
（定义 MVP 核心闭环与验收标准，见 `project/01-任务看板.md`）。

## 预期会有的东西

| 文件 | 内容 | 什么时候该有 |
|---|---|---|
| `00-产品大图.md` | 长期形态、用户是谁、不做什么 | 启动时 |
| `01-MVP范围.md` | 第一版做什么、**明确不做什么** | T-004 |
| `02-核心闭环.md` | 上传 → 分析 → 看到什么 → 下次改什么 → 是否改善 | T-004 |
| `03-验收标准.md` | 每条都要能写成一个测试，数字指到评测集或线上指标 | T-004 |
| `04-指标定义.md` | 每个指标怎么算、从哪取数、多久看一次 | T-004 之后 |
| `prd/` | 逐个功能的需求文档 | 进入开发前 |

## 两条硬约束

1. **`专家假设` 等级的知识不能进产品自动输出**——知识库规范里写死的。
   写需求前先确认引用的单元是什么等级、`review.status` 是什么。
2. **「知识说得出」不等于「系统测得出」**。前者看 `climbing-kb/`，
   后者看 `annotator/README.md` 的可信度那几节和
   `climbing-kb/perception/observable-capability-map.yaml`。
