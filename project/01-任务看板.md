# 任务看板

> 项目经理独占写。最后更新：2026-08-29

状态：`待办` / `进行中` / `阻塞` / `待复核` / `完成`

## 进行中

| ID | 任务 | 负责 | 依赖 | 完成标准 |
|---|---|---|---|---|
| T-006 | 建立回归基线：固化测试输入，为关键点之后的计算建黄金快照，把已知的静默失败变成会响的测试 | qa | 无（不依赖 T-005；反过来 T-005 需要它做安全网） | `qa/用例/` 一条命令可全跑；`qa/回归基线/` 有黄金快照且能检出数值变化；`qa/报告/` 含「本次没有验证什么」一栏 |
| T-007 | 修复 D-007（`review.py` 崩溃）与 D-001（陈旧目录只告警不阻断） | engineer | 无（缺陷已录入 `qa/缺陷清单.md`，源自 T-006） | `review.py` 对 `qa/fixtures/out7/evidence.jsonl` 不再崩溃且空切片指标标为不可用而非填 0；`make_case --update-measured` 在目录陈旧时硬失败，`compare.py`/`card.py` 维持告警；`./qa/用例/run.sh` 跑通并如实报告 XPASS 情况 |

> 本轮 `qa/缺陷清单.md` 共 9 条（D-001~D-009），T-007 只覆盖 D-007、D-001。
> 其余 7 条本轮不排任务，去向仍是 `qa/缺陷清单.md`：
> D-002（中）、D-003（中，工程师+研究专家）、D-004（高·潜伏）、D-005（中）、
> D-006（高）、D-008（低）、D-009（低，研究专家）。

## 待办 · 本轮启动任务

| ID | 任务 | 负责 | 依赖 | 完成标准 |
|---|---|---|---|---|
| T-001 | 建立项目总表与日报模板 | pm | — | 总表、看板、决策记录、风险清单、日报模板齐备且被 Routine 驱动 |
| T-002 | 建设动作评测集：成功／失败／易误判各不少于 5 例 | research | T-005 提供可复现管线 | 案例单元 `measured` 全部来自管线实测，validate 通过 |
| T-003 | 建设知识来源渠道 | research | — | 有一份可持续的来源清单与准入标准，不是一次性抓取 |
| T-004 | 定义 MVP 核心闭环与验收标准 | product | T-002 的评测集雏形 | 闭环五步写清楚，每条验收标准都能写成测试 |
| T-005 | 把视频标注整理成可重复运行的技术验证流程 | engineer | 建议排在 T-006 之后或与之并行——没有回归基线，改管线时无法判断有没有改坏东西 | 对新素材一条命令跑通，输出显式，含已知限制 |
| T-008 | 更新缺陷清单的 XFAIL→XPASS（D-007、D-001 修复后关闭对应记录） | qa | T-007 | `qa/缺陷清单.md` 中 D-007、D-001 的「状态」列改为已修复；`./qa/用例/run.sh` 不再因这两条缺陷判 XPASS 失败 |

## 阻塞

| 谁 | 卡在什么上 | 需要谁 | 起始 |
|---|---|---|---|
| qa 的 T-006 | 测试输入不持久。`annotator/.gitignore` 含 `out*/`，`out5/` `out7/` 零个文件被 git 跟踪；源视频位于容器级的 `/root/.claude/uploads/` 下，会随容器回收消失。输入一旦消失基线无法复现。已定的解法是把 `out5/out7` 的 keypoints.npz / evidence.jsonl / holds.json / summary.json（两个目录合计约 535KB，不含影像）固化到 `qa/fixtures/`。 | qa 自己执行；**是否提交原始视频以覆盖 pose/holds/contact 这一层，需要人决定** | 2026-08-28 |
| research 的知识证据升级（新卡从 `专家共识` 升到 `研究证据`） | R-008：出站网络策略拦截 PubMed／PubMed Central／BMC，`WebFetch` 对这些域名返回 `EGRESS_BLOCKED`，新来源只能停在「候选」，达不到 `研究证据` 等级所需的可访问链接 | 需要人修改环境出站配置，放行 `pubmed.ncbi.nlm.nih.gov`、`pmc.ncbi.nlm.nih.gov`、`thebmc.co.uk` 等域名 | 2026-08-29 |

## 待复核（等真人攀岩专家）

| 对象 | 内容 | 提交时间 |
|---|---|---|
| 全部 66 个知识单元 | `review.status` 均为 `pending` | 2026-08-28 |
| qa 的产出（测试计划、基线、缺陷清单） | 尚无人复核 | 2026-08-28 |

> 复核是外部资源，不是五个 agent 能自己完成的环节。
> 这一栏长期堆积就是项目的头号风险。
>
> 上一行（qa 产出）不要与 R-001 混为一谈：那一条是攀岩知识需要真人攀岩专家背书，
> 这一条是工程质量（测试设计是否合理、基线是否可信），不需要真人攀岩专家，
> 由主会话或工程师交叉看即可。

## 完成

| ID | 任务 | 负责 | 完成时间 |
|---|---|---|---|
| T-000 | 四角色机制搭建（agent 定义 + 共享文件 + 定时任务） | 主会话 | 2026-08-28 |
