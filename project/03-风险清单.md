# 风险清单

> 项目经理独占写。最后更新：2026-08-29

概率 / 影响：高 · 中 · 低

| ID | 风险 | 概率 | 影响 | 缓解 | 负责 | 状态 |
|---|---|---|---|---|---|---|
| R-001 | **零个知识单元通过真人攀岩专家复核**，整个知识底座无人背书；若复核后大面积推翻，产品与工程的返工量不可控 | 高 | 高 | 已知悉并接受；MVP 不做自动判断以规避（D-004） | pm 推动，外部专家执行 | 已接受（D-003） |
| R-002 | **评测集只有 2 个案例**，工程能力无法被客观验收，「效果变好了」无法证伪 | 高 | 高 | T-002：成功／失败／易误判各不少于 5 例 | research | 已排入本轮 |
| R-003 | **没有真实用户**，链路第 ④ 环（用户反馈回流）完全为空；知识库只会越来越自洽，不会被现实纠错 | 高 | 高 | MVP 定义阶段就要把「怎么拿到失败案例」设计进去，而不是上线后再想 | product | 未缓解 |
| R-004 | 感知管线的结论对**输入目录**敏感（同一视频两次运行，关键结论从 −0.50 变成 −0.38），已加陈旧告警但依赖人去看 | 中 | 中 | 把陈旧检查升级为硬失败而非告警；产物落盘时记录来源目录与管线版本 | engineer | 部分缓解 |
| R-005 | 教学视频来源之间已出现**互相矛盾的说法**（非承重腿该用多大力）。若不断累积而无人裁决，知识库会变成一堆并列的说法 | 中 | 中 | 分歧如实并列已是既定做法；需要专家复核来裁决，否则只能长期挂着 | research → 外部专家 | 已记录未裁决 |
| R-006 | 四个 agent 的产出**只在文件里同步**，若某个角色跳过「写状态文件」这一步，其余角色与日报都会读到过期信息 | 中 | 中 | 各 agent 定义里写死了收尾动作；pm 在日报里核对三份状态文件的更新时间 | pm | 已缓解 |
| R-007 | **转录与提炼失真**——与来源质量无关的独立风险。已知实例：字幕为创作者自动生成且实测有错（「眼点」应为「脚点」）；一条视频在 82 秒被截断导致第二条「秘诀」缺失；英文视频的中文字幕为机翻术语不可用；全部内容抠自内嵌字幕而非音轨转录 | 高 | 中 | 让 research 系统回溯每个「专家共识」单元的主张到 `sources/` 原文，区分「来源原话」与「我的提炼」 | research | 未缓解 |
| R-008 | **知识获取渠道在本环境不可执行**——`climbing-kb/docs/09-知识获取渠道.md` 规定的固定渠道 PubMed、PubMed Central、BMC（`thebmc.co.uk`）在 `WebFetch` 下全部返回 `EGRESS_BLOCKED`（连同 mdpi、sciencedirect、wikipedia 等镜像），仅搜索工具可用。A1 轮 3 条研究来源（Noé 2006、Quaine & Martin 1999、Quaine/Martin/Blanchi 1997）一个字原文都没打开，只有搜索引擎二手摘要；按 `climbing-kb/docs/00-知识库规范.md` 证据等级规则第 2 条（标 `研究证据` 必须给可访问链接），新增单元因此降标 `专家共识`，BMC 一手教学材料本轮为 0。会持续压低后续每一轮新卡的证据等级上限。**代理层证据**（`project/环境网络实测-2026-08-28.md`，主会话 2026-08-28 实测）：curl 直连三个域名均 HTTP 000／`connect failed`；代理状态 `recentRelayFailures` 记录 3 条 `connect_rejected`，detail 均为 `gateway answered 403 to CONNECT (policy denial or upstream failure)`，host 为 `pubmed.ncbi.nlm.nih.gov:443`、`pmc.ncbi.nlm.nih.gov:443`、`www.thebmc.co.uk:443`（environment_id `env_01Fo7oXZBQRA6SaRvnf1FUuP`，名称 Leah）。区别有实际意义：`EGRESS_BLOCKED` 只说明 `WebFetch` 拿不到，代理日志证明拒绝发生在代理网关而非目标站点，解法明确是改环境网络策略放行域名，换检索工具或重试都无效 | 高 | 高 | 需要人放行上述域名的出站访问（研究专家已在状态文件「给主会话/人」一节提出请示）；缓解前只能维持 `专家共识` 上限 | research → 人 | 已上报，待处理 |
| R-009 | **感知前端（`pose.py`／`holds.py`／`contact.py`）无回归保护**——原始视频不进 git（人的决定），`qa` 的回归基线读的是固化的旧 npz；MediaPipe 换版本、ROI 策略改坏、接触阈值调错，`./qa/用例/run.sh` 一条测试都不会响（检出率从 100% 掉到 46.7% 也会全绿）。这是「不提交原始视频」这一决定的直接代价，对应 `project/01-任务看板.md` 阻塞区 qa 的 T-006 条目 | 高 | 中 | 已知悉并接受；待补替代手段（如合成/半合成输入、抽样人工复核） | qa／engineer | 已知并接受，待补替代手段 |
| R-010 | **脚—岩点关联率为 0**——研究专家 A1 轮实测：`out7` 左脚 0/29、右脚 0/77 接触帧，`out5` 为 0/199（左）与 1/210（右）；同批素材手是 19/20 与 69/70。管线因此分不出「同点换脚／换到另一个点／脚脱落」，是 A1 主题（换脚与支撑交接）的核心观察量硬阻塞；现有两段素材里没有一次同点换脚，评测样本需另找 | 高 | 高 | 未缓解——需工程侧补脚与岩点的关联判定能力，同时需要含同点换脚片段的新素材 | engineer（管线）＋ research（素材） | 未缓解 |
